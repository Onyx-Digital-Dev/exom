// Exom Theme - Professional, minimal, dark

export global Theme {
    // Core colors
    out property <color> background: #0e1116;
    out property <color> surface: #161b22;
    out property <color> surface-elevated: #1c2129;

    // Accent colors
    out property <color> accent: #5a7da8;
    out property <color> accent-muted: #3d556e;
    out property <color> border: #3a5070;
    out property <color> border-subtle: #2a3a4a;

    // Text colors
    out property <color> text-primary: #e6edf3;
    out property <color> text-secondary: #8b949e;
    out property <color> text-muted: #6e7681;

    // Status colors
    out property <color> online: #3fb950;
    out property <color> offline: #6e7681;
    out property <color> error: #f85149;
    out property <color> warning: #d29922;

    // Dimensions
    out property <length> border-width: 1px;
    out property <length> spacing-xs: 4px;
    out property <length> spacing-sm: 8px;
    out property <length> spacing-md: 12px;
    out property <length> spacing-lg: 16px;
    out property <length> spacing-xl: 24px;

    // Panel widths
    out property <length> left-panel-width: 220px;
    out property <length> right-panel-width: 240px;

    // Font sizes
    out property <length> font-xs: 11px;
    out property <length> font-sm: 12px;
    out property <length> font-md: 13px;
    out property <length> font-lg: 15px;
    out property <length> font-xl: 18px;
}
